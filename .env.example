# This project requires no API keys or secrets — it runs entirely on local,
# static data with DuckDB (an embedded database, no server/credentials).
#
# This file is kept as a placeholder in case you later add optional
# integrations (e.g. a live data source, or OLLAMA_HOST for the optional
# local-LLM explanation layer described in the README).

# OLLAMA_HOST=http://localhost:11434
